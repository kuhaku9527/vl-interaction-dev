# 决策书（JoyAI-VL-Interaction 项目真值源）

> 本目录是项目**已确定决策的真值源**（"下锤的事实清单"）。AI 跨会话不丢决策的落盘点。
>
> ⚠️ **证据链状态（2026-09-14 核查）**：本目录原名列的两项溯源配套**均已失效**，见 §1.1。
> 自检请跑：`/d/AI/envs/joyai-main/python.exe scripts/doc_health.py --check ssot`

---

## §0 修改治理协议（最高优先级，强制）

1. **任何条目增删改必须走流程**：① AI 提出具体改动（reason + 建议日期 + 证据）→ ② 用户明确同意 → ③ 才落盘，并在条目末追加 `modified: 日期｜by AI｜approved: 用户`。
2. **🔒 锁死条目**：除治理流程外禁止改动。🔓 软锁条目（标 `待 #XX`）同样走流程，但允许在对应 PR 合入后由 AI 提议关闭。
3. **冲突裁决**：本目录与 `MEMORY.md`/记忆冲突时，**以本目录为准**；反向修正记忆。
4. **本轮（2026-07-28 召回轮）特许**：用户明确"这一版不用我确定、人工逐条确认不现实"，故本轮全量细分撰写**由 AI 直接落盘**（不起子代理、逐条 git/代码核实）。此后任何修改恢复 §0.1 流程。
5. **生产路由（防多端污染）**：各端对话**只写 spec+adr 文件，绝不读其他端对话**——ADR 落 `doc/adr/`，spec 落 `doc/specs/`。**唯一写者=审查组对话**：由用户手动触发，召回 `doc/specs/`+`doc/adr/` 交叉验证（git + 代码 + spec/adr + 日期记忆）后汇编成本目录条目**提案**，按 §0.1 经用户批准才落盘。端点仍须读本目录(SSOT) 但不得互读对话。
6. **指针卫生（2026-09-14 新增，防复发）**：新增/修改条目时，**禁止**把行号（`file.py:123`）写进「校验」列——行号漂移极快，本目录已有 15 条因此失效。改用**关键词 grep**（如 `grep -n "request_timeout_seconds" path`），或 `config/drift-contract.json` 的 check id。移动任何文件后必须全仓扫旧路径，否则制造死链。

---

## §1 召回方法论（2026-07-28 确立，防再漂移）

- **亲力亲为，不起子代理**：子代理会被压缩且无权写盘，不能委托决策。本轮所有 git/代码/记忆核实由主理人直接执行。
- **三源交叉验证**：每条决策日期取自 **git 提交时间**（最权威）＋**ADR/报告原文 Date 字段**＋**代码注释/日志**；运行态与决策态**分开记**。
- **运行态 ≠ 决策态**：配置/启动逻辑写死的值（决策态）与当前进程实际值（运行态）可能背离，凡背离记入 `Drift` 列，不污染决策日。
- **真实溯源日**：`D-YYYY-MM-DD-NNN` 的日期 = 该决策**实际确立或合入日**，不是文档撰写日。（⚠️ 原注"git 历史最早仅到 `d75faf6`"已失效，见 §1.1）
- **对话记录召回局限**：2026-07-28 那轮召回时，历史对话检索工具返回 0 结果（5 次查询均空），跨会话内容只能靠 `reports/` 与每日日志回溯。**该工具属当时所用的 agent 环境，当前环境（DSH）无此能力**，见 `doc/environment-dsh.md`。

### §1.1 ⚠️ 证据链断裂说明（2026-09-14 核查）

本目录建立时的两项溯源支柱**均已失效**，重建自检能力前，条目的「来源」列**只能被信任、无法被验证**：

| 支柱 | 原状 | 现状 | 影响 |
|---|---|---|---|
| **commit SHA** | §1 称"git 提交时间（最权威）" | **约 40+ 条引用的 SHA 在对象库中全部不存在** | 仓库于 2026-08-02 做过 `git filter-repo` 历史重写（见 `.git/filter-repo/`），旧 SHA 整体失效。抽样 32 个，31 个 MISSING |
| **`decision-trace/`** | 原第 4 行标注为"配套溯源证据" | **不存在，且从未进版本控制** | 该目录原在 `.workbuddy/`（`.gitignore:19` 忽略），任何 clone/换机即永久丢失。原称含 `decision-inventory.md`（L1–L4 约 60 条证据） |

**替代锚点（推荐）**：用 **PR 号** 反查（`git log --grep="#83"` 有效），比 SHA 稳；或直接用**文件路径 + 关键词**（不依赖行号）。

**尚未处理的遗留**：`决策/业务-上下文架构.md:81` 引用的 `决策/tmp/decision-trace/trace-adr.md` 同样不存在（路径还多一层 `决策/`）<!-- known-absent -->；`决策/启动链路.md:3` 引用 `decision-inventory.md` 作为"细分依据"。

> 处置原则：**不删原条目**（历史决策仍有效），但「来源」列引用失效 SHA 的条目应视为「来源不可复核」。全量清单由 `scripts/doc_health.py --check ssot` 输出。

---

## §2 文件索引（按 L1-L4 细分）

| 层 | 文件 | 覆盖 D-id（2026-09-14 实测校正） | 状态 |
|---|---|---|---|
| L1 | [`启动链路.md`](启动链路.md) | D-2026-07-11-001 + D-2026-07-13-002~007 + D-2026-07-28-008 + D-2026-08-04-001 | ✅ 细分 |
| L4 | [`跨域铁律.md`](跨域铁律.md) | D-2026-07-13-015 + D-2026-07-23-011/012/018 + D-2026-07-24-017 + D-2026-07-26-010 + D-2026-07-27-013/014/016 + D-2026-08-01-019 + D-2026-08-01-082~086 | ✅ 细分 |
| L4 | [`AI代码质量约法三章.md`](AI代码质量约法三章.md) | 非 D 编号铁律(2026-08-03) | ✅ 新增 |
| L2 | [`服务-VLM.md`](服务-VLM.md) | D-2026-07-11-024 + D-2026-07-13-020~023/025~027 | ✅ |
| L2 | [`服务-webinfer.md`](服务-webinfer.md) | D-2026-07-13-023/024 + D-2026-07-21-026~028 + D-2026-07-22-025/029 + D-2026-07-24-030/031 + D-2026-07-29-032~035 | ✅ |
| L2 | [`服务-webui.md`](服务-webui.md) | D-2026-07-13-032 + D-2026-07-21-033 + D-2026-07-23-037/038 + D-2026-07-24-034/039 + D-2026-07-26-035 + D-2026-07-27-036 + D-2026-08-03-004 | ✅ |
| L2 | [`服务-日志.md`](服务-日志.md) | D-2026-08-01-060~062 | ✅ |
| L2 | [`服务-memory-store.md`](服务-memory-store.md) | D-2026-07-24-033 + D-2026-07-25-032/034/035 + D-2026-07-26-030/031/037 + D-2026-07-27-036 + D-2026-08-05-001~003 | ✅ |
| L2 | [`服务-语音栈.md`](服务-语音栈.md) | D-2026-07-11-045~047 + D-2026-07-13-045 + D-2026-08-08-080 | ✅ |
| L2 | [`服务-Hermes.md`](服务-Hermes.md) | D-2026-07-23-048 | ✅ |
| L2 | [`服务-background-agent.md`](服务-background-agent.md) | D-2026-07-23-049 | ✅ |
| L3 | [`业务-LocalWiki.md`](业务-LocalWiki.md) | D-2026-07-24-040 + D-2026-07-25-041/042/048 + D-2026-07-26-043/047 + D-2026-07-27-044~046 + D-2026-08-05-003 | ✅ |
| L3 | [`业务-决策记忆.md`](业务-决策记忆.md) | D-2026-07-13-075 + D-2026-07-28-076 | ✅ |
| L3 | [`业务-审计收口.md`](业务-审计收口.md) | D-2026-07-23-077 + D-2026-07-24-078 | ✅ |
| L3 | [`业务-上下文架构.md`](业务-上下文架构.md) | D-2026-07-13-027/050/051 + D-2026-07-23-052~055 | ✅ |
| L3 | [`业务-评测.md`](业务-评测.md) | D-2026-07-25-081 + D-2026-08-05-003 | ✅ |
| L4 | [`模型与权重.md`](模型与权重.md) | D-2026-07-13-011 + D-2026-07-28-012 | ✅ |
| L4 | [`交互模式与决策token规范.md`](交互模式与决策token规范.md) | D-2026-08-03-001~003 + D-2026-08-17-001 | ✅ 新增 |
| L4 | [`调研-HF-speech-to-speech-姿态.md`](调研-HF-speech-to-speech-姿态.md) | D-2026-08-06-001 | ✅ 新增 |
| L4 | [`spec编写规范.md`](spec编写规范.md) | 非 D 编号铁律(2026-08-06) | ✅ |
| — | [`VLM架构与模型组成.md`](VLM架构与模型组成.md) | 分离式双模型权威真值 + 漂移纠正（2026-08-04） | ✅ 新增 |
| L4 | [`工程规范.md`](工程规范.md) | D-2026-07-20-007 + D-2026-07-22-008 + D-2026-07-26-006 + D-2026-08-03-003 | ✅ |
| — | [`drift-历史.md`](drift-历史.md) | 集中 🔓 Drift（6 条：5 闭环 / 1 悬空） | ✅ |
| — | [`scripts/doc_health.py`](../scripts/doc_health.py) | **文档库健康检查**（死链/SSOT 指针/索引覆盖/零引用） | ✅ 2026-09-14 新增 |

> ⚠️ **原表 D-id 范围已失真**（2026-09-14 修正）：此前 8 个文件的范围标注全部与实际不符，例如 `业务-LocalWiki.md` 标 `D-060~074` 实为 `040~048`，`服务-VLM.md` 标 `D-020/021/022/050` 实为 `020~027`。上表已按实测重写。
>
> ⚠️ **原注的"完整细分清单见 `.workbuddy/tmp/decision-trace/decision-inventory.md`"已删除**——该文件不存在且从未进版本控制（见 §1.1）。
>
> **条目统计（2026-09-14）**：24 个文件、**103 条唯一 D-id**、49 个 `### D-` 标题。其中带条目级 `modified:` 落款的仅 **14 条（13.6%）**，且 §0 协议生效后新增的条目（含最新 `D-2026-08-17-001`）多数无落款——**落款纪律实际执行率低**，这是本机制最薄弱的一环。
>
> **重复 D-id（需消歧）**：`D-2026-07-13-023` 同时存在于 `服务-VLM.md` 与 `服务-webinfer.md`；`D-2026-07-27-036` 同时存在于 `服务-memory-store.md` 与 `服务-webui.md`。交叉引用"见 D-023"无法判定指哪个。

---

## §3 条目七字段格式

每条决策固定七字段：

```
## D-YYYY-MM-DD-NNN  标题
- **事实**: 一句话确定事实
- **来源**: git commit / PR# / ADR-XXXX / 日志 / 代码位置
- **校验**: 可重跑命令（curl/grep/py_compile 等），用于 verify.sh
- **预期**: 校验应满足的断言
- **Drift**: 运行态背离（若有），含发现日+根因+修复
- **Owner**: 责任端（DevOps/前端/后端/ML/架构）
- **锁定**: 🔒 锁死 / 🔓 软锁(待 #XX)
```

---

## §4 当前已知 Drift

> ⚠️ **2026-09-14 重核**：本节此前**停留在 2026-07-28 状态**，4 条中 3 条已闭环却仍标"当前已知"，其中第 3 条还把 opt-out 写成了 opt-in（语义反转），会误导后人去修不存在的问题。
> **纪律（防复发）**：本节**不再手工维护第二份 Drift 清单**。Drift 状态的唯一真值源是 [`drift-历史.md`](drift-历史.md)；本节只保留"仍需动作"的条目，闭环后立即移出。

**当前仍需动作（1 条）**

1. **DRIFT-5：KWS 训练脚本路径外溢**（2026-07-28 记录，至今未动）——`services/scripts/{assemble_kws_corpus,train_kws,build_negative_pool}.py` 默认写 `/mnt/d/AI/data/kws/...` 与 `D:/AI/data/kws/...`，越过工作区边界。非阻塞，建议整改。详见 `drift-历史.md` DRIFT-5。

**已闭环（保留日期备查，不再列为"当前"）**

| # | 主题 | 闭环 |
|---|---|---|
| DRIFT-1 | VLM n_ctx 运行态回退 4096 | ✅ 2026-07-29（`/props` 报 `n_ctx=16384`） |
| DRIFT-2 | memory-store 端口 8996 | ✅ 2026-07-29（`run-windows.env:28-31` 四行覆盖；`run-windows.ps1:109` 默认 8997） |
| DRIFT-3 | webui 网关默认 8996 | ✅ 2026-07-29（`admin_endpoints.py:45` 默认 8997） |
| DRIFT-4 | bge-m3 provider 被 #38 改 nvidia（无 ADR） | ✅ 2026-07-28（#42 revert 回 `local`） |
| DRIFT-6 | chat 被 memory-store 拖累 | ✅ 2026-07-29 |

> **更正（原第 3 条的语义错误）**：原文称"memory-store 默认 **opt-in**（`JOYAI_ENABLE_MEMORY_STORE=1`）"——**方向反了**。`run-windows.ps1:658` 实为 `if ($env:JOYAI_ENABLE_MEMORY_STORE -ne "0")`，即**默认 ON、opt-out**。
