# 跨域铁律 / Cross-Domain Doctrines

> 范围：横扫多域的"绝对不能违反"规则。每条都是踩过坑、漂移过、痛过之后定下来的。
> 跟具体服务/具体业务绑定的确定项，**不**在此处——那些去对应 domain 文件。
> **修改走 §0 治理协议（AI 提议 → 用户同意 → 落盘）。**

---

### D-2026-07-26-010 | 端口铁律（memory-store 真值）
| 字段 | 内容 |
|---|---|
| **事实** | **`127.0.0.1:8997` = memory-store / bge-m3 语义召回后端**（真数据）；`127.0.0.1:8996` = 废弃空壳，禁止再起 |
| **来源** | 8997 确立为生产后端于 2026-07-26（`服务-memory-store.md` D-030/D-031）；`run-windows.ps1:114` 默认仍 8996 |
| **校验** | `curl -fsS http://127.0.0.1:8997/health -m 2 && echo "8997 alive"; curl -fsS http://127.0.0.1:8996/health -m 2 2>&1 | head -1` |
| **预期** | 8997 200 OK；8996 连接失败或 404 |
| **Drift** | 2026-07-26 误以为 8996 是真后端，导致前端 F2/F3 全部 502 |
| **Owner** | 审查协调 |
| **锁定** | ✅ |

---

### D-2026-07-23-011 | 工作区隔离（禁止外溢磁盘根）
| 字段 | 内容 |
|---|---|
| **事实** | 工作区 = `D:\AI\workspace\JoyAI-VL-Interaction-main`；缓存 → `<workspace>/.cache/`；草稿 → `<workspace>/.workbuddy/tmp/`。**禁止**写到 `D:/c` `D:/d` `D:/Cache` `D:/tmp`（git-bash 下 `/tmp` = `D:/tmp`） |
| **来源** | `doc/standards/workspace-isolation.md` + `scripts/guard-workspace-paths.ps1`（纪律确立于 2026-07-23 审计期；2026-09-14 校正：仓库只有 `.ps1` 版，原引用的 `.sh` 版从未落盘） |
| **校验** | `pwsh -File scripts/guard-workspace-paths.ps1` |
| **预期** | 退出码 0（无外溢） |
| **Drift** | 2026-07-23 误写 `D:/tmp/start-stack.sh`、误写 `D:/Cache/...` 多次事故 |
| **Owner** | 全员 |
| **锁定** | ✅ |

---

### D-2026-07-23-012 | 多对话并发 = 独立 worktree
| 字段 | 内容 |
|---|---|
| **事实** | 多个 WorkBuddy 对话同时改码必须各自一个 git worktree；**禁止**共享同一工作树乱写 |
| **来源** | `doc/standards/workspace-isolation.md`（2026-07-23 确立） |
| **校验** | `git worktree list` |
| **预期** | 每个对话对应一行 `/path [<branch>]` |
| **Drift** | 2026-07-23 多对话同 worktree 改码，缓存/草稿互相覆盖 |
| **Owner** | 全员 |
| **锁定** | ✅ |

---

### D-2026-07-27-013 | 沙箱 git 写入陷阱（实测发现）
| 字段 | 内容 |
|---|---|
| **事实** | WorkBuddy 沙箱里 `git stash push` 会**丢弃分支 ref**；`git checkout`/`merge` 大量写文件时**部分文件静默丢失**（工作树被部分删除）。**保留未提交改动勿用 `git stash`** |
| **来源** | 实测 2026-07-27（B3/B4 翻案当晚，工作树被静默删 39 文件）；`服务-memory-store.md` Drifts |
| **校验** | 走"先验证再做事"流程：`git status --porcelain <path>` 对比 `git show HEAD:<path>` |
| **预期** | N/A（防御性规则） |
| **Drift** | 🟥 2026-07-27 stash 后分支 ref 丢失；checkout/merge 后 39 文件被静默删（已用 `git checkout HEAD --` 还原） |
| **Owner** | 全员 |
| **锁定** | ✅ |

---

### D-2026-07-27-014 | git 还原用 HEAD 而非索引
| 字段 | 内容 |
|---|---|
| **事实** | 工作树被沙箱陷阱静默删除（且状态是 staged `D`/`M`）时，`git checkout -- <path>` 是**空操作**（从索引还原，索引已被改）。必须用 `git checkout HEAD -- <path>`（从 HEAD 同时修索引+工作树）才有效 |
| **来源** | 实测 2026-07-27 22:45（首次用 `git checkout --` 退出码 0 但零还原；改 `git checkout HEAD --` 成功） |
| **校验** | `git status --porcelain <path> | wc -l` 应为 0 |
| **预期** | 0 行（磁盘 == HEAD） |
| **Drift** | 🟥 2026-07-27 第一次用 `git checkout --` 退出码 0 但**零还原**；改 `git checkout HEAD --` 成功 |
| **Owner** | 全员 |
| **锁定** | ✅ |

---

### D-2026-07-13-015 | 超时配置（三条独立链路，非"前端30s vs 后端300s"）
| 字段 | 内容 |
|---|---|
| **事实** | 三个**不同链路**的超时，互不替代： |
| | ① webinfer → VLM 推理 `request_timeout_seconds = 300.0`（5min）：`services/webinfer/adapter_types.py:75` |
| | ② webui 网关 → memory-store 代理 `aiohttp.ClientTimeout(total=30)`：`services/webui/src/joy_interaction_webui/server.py:976` |
| | ③ **前端 `wiki_frontend.js` 无任何 abort/timeout**（fetch 不超时，靠后端响应） |
| **来源** | `adapter_types.py:75` + `server.py:976` + `wiki_frontend.js`（全树 grep 无 `setTimeout/abort` 于 LLM 调用） |
| **校验** | `grep -n "request_timeout_seconds" services/webinfer/adapter_types.py; grep -n "ClientTimeout(total=30)" services/webui/src/joy_interaction_webui/server.py` |
| **预期** | 命中 `300.0` 与 `total=30` |
| **Drift** | 🟥 早期文档误写成"前端 abort 30s vs 后端 300s"——实际前端无 timeout，30s 是网关代理超时，与 300s 非同一链路；用户报"[LLM error.] 30s"根因是 VLM 死 + 网关 30s 代理超时先到 |
| **Owner** | 前端 + 后端 |
| **锁定** | 🔓（待对齐：是否给前端加 abort + 心跳；代理 30s 是否需拉大） |

---

### D-2026-07-27-016 | GitHub 推送必走 gh CLI（绕沙箱）
| 字段 | 内容 |
|---|---|
| **事实** | WorkBuddy Bash 默认 sandbox 拦截 git/gh 网络。`git push` 走 gh-proxy `insteadOf` 重写会被 `Connection reset`。**可靠路径**：用 `gh` CLI（绕沙箱）+ `dangerouslyDisableSandbox:true` |
| **来源** | 实测 2026-07-27 起（`gh` 登录态 kuhaku9527，repo scope）；push 路径 07-28 修正 |
| **校验** | `gh auth status` |
| **预期** | `Logged in to github.com as kuhaku9527` |
| **Drift** | 多次 push 失败 → 改 `git config --local --unset-all 'url.https://gh-proxy.com/...insteadof'`（local+global 两层、小写键）绕开 |
| **Owner** | 全员 |
| **锁定** | ✅ |

---

### D-2026-07-24-017 | Lint / CI 门禁（ADR-0011）
| 字段 | 内容 |
|---|---|
| **事实** | 根 `pyproject.toml` repo-wide ruff，CI 装 `ruff==0.15.22`；`quality.yml` 跑 ruff(check+format --check)+eslint(webui)+package-smoke(webinfer)。**只 check 绿≠门禁绿**，必验 `ruff format --check` |
| **来源** | ADR-0011（Batch 3 实施于 2026-07-24）+ `.github/workflows/quality.yml` |
| **校验** | `ruff format --check .` |
| **预期** | 0 个 unreformated |
| **Drift** | 早期合 PR 只看 `ruff check`，format 漂移 |
| **Owner** | 审查协调 |
| **锁定** | ✅ |

---

### D-2026-07-23-018 | 报告必须分四章
| 字段 | 内容 |
|---|---|
| **事实** | 写代码质量 / 测试 / 体检类报告**必须按角色分四章**：`前端` / `后端` / `测试` / `架构`；每章「发现 + 该角色该做的事」；跨角色项两章都提标「共担」 |
| **来源** | 审计纪律（2026-07-23 起，`reports/code-health-audit-20260723.md` 即四章结构） |
| **校验** | `grep -c "^## " reports/code-health-audit-20260723.md`（应 ≥ 4） |
| **预期** | ≥ 4 个二级标题 |
| **Drift** | 早期报告合在一章，跨端没法认领 |
| **Owner** | 审查协调 |
| **锁定** | ✅ |

---

### D-2026-08-01-019 | Drift Gate 门禁（运行态↔决策态一致性；v1 执行器已撤、原则保留）
| 字段 | 内容 |
|---|---|
| **事实** | **运行态必须与决策态一致**（drift-gate 核心原则）：任何改动不得让运行态（进程实际值 / 启动逻辑写死值）背离 `决策/` 真值源。v1 自动执行器（`drift_gate.py`）已于 `0c22913`（PR #49，main `484cab5`）撤除（废弃流程债）；**v2 轻量门禁已落地**（`quality.yml` 的 `drift-gate` job）。原则保留 + 自动门禁恢复。<br>⚠️ 2026-09-14 校正：原述「job 跑 `bash scripts/verify.sh --ci`」已失效——`--ci` 参数已被移除（现 `usage: verify.sh [--quiet]`），**静态断言已迁至 `scripts/drift_gate.py` + `config/drift-contract.json`**。 | <!-- known-absent: 0c22913 因 2026-08-02 filter-repo 失效 -->
| **来源** | PR #49（`0c22913`）<!-- known-absent: SHA 因 2026-08-02 filter-repo 失效 -->；收束报告 `reports/codex-handoff-closure-2026-08-01.md` §4 <!-- known-absent: 该报告不在 HEAD，仅存在于 tag archive/feat-drift-gate-runtime-v2.1 与 archive/feat-q2-emit -->；ADR-0012（Drift Gate 评审反馈）；`决策/服务-日志.md` 服务集合曾含 `drift-gate`；v2 落地于 `quality.yml` `drift-gate` job + `scripts/drift_gate.py` |
| **校验** | CI 自动：`python scripts/drift_gate.py --contract config/drift-contract.json --phase static --mode closed`（任一 FAIL 即非零退出阻断合并；2026-09-14 复跑 **9/9 OK，exit 0**）；**运行时/端口检查由 v2.1 `drift-gate-runtime` job fail-closed**（起 webui:8099+memory-store:8997 跑全量 `verify.sh`，废弃 :8996 反向断言）。人工评审 checklist 仍保留作双保险 |
| **预期** | `drift_gate.py --phase static` 全 OK（2026-09-14 实测 9/9，block_fail=False，退出码 0；`bash scripts/verify.sh` 运行态探针亦无 FAIL）；原「2 已知 DRIFT」(DRIFT-2 memory-store 端口 8996 / DRIFT-3 webui 网关默认 8996) 已于 2026-07-29 闭环并写入契约 `memory-store-port`(block)/`webui-gateway-port`(warn)，非持续漂移。PR 评审仍确认无新增 Drift，新增背离记 `drift-历史.md` |
| **Drift** | 🟥 2026-08-01 v1 执行器被删 → 自动兜底空缺；已由 v2 轻量门禁（`quality.yml` `drift-gate` job）补上；运行时/端口检查由 v2.1 `drift-gate-runtime` job（PR #53）fail-closed 补上 |
| **Owner** | 审查协调 |
| **锁定** | 🔒（v2 轻量门禁已落地；v2.1 运行时门禁已落地：`drift-gate-runtime` job 起 webui:8099+memory-store:8997 跑全量 verify.sh，废弃 :8996 反向断言 fail-closed，PR #53） |
| **modified** | 2026-08-01｜by AI｜approved: 用户（v2 轻量门禁 approved，「轻量接 CI」）；2026-08-01｜by AI｜approved: 用户（v2.1 运行时门禁落盘，PR #53） |
---

### D-2026-08-01-086 | Drift Gate v2.1 运行时门禁（drift-gate-runtime job）
| 字段 | 内容 |
|---|---|
| **事实** | Drift Gate v2.1 阶段A 运行时门禁已落地：`quality.yml` 新增 `drift-gate-runtime` job。**刻意不 `needs` 任何 job、与静态门禁并行独立跑**——避免上游红时被 skipped 导致 runtime 门禁从未在 CI 真跑过（PR #53 run 30703927856 实测 skipped 教训）。PR 触发时起 memory-store(:8997)+webui(:8099) 两 CPU 服务，poll `/health` 200 后跑全量 `bash scripts/verify.sh`，对 webui/memory-store 端口检查（含废弃 :8996 反向断言 `check_port_absent`）fail-closed 拦合并；其余 6 服务未起→[DOWN] 不计 fail。`drift-gate` job 现跑 `scripts/drift_gate.py --phase static`（原 `--ci` 参数已移除） |
| **来源** | PR #53（`feat/drift-gate-runtime-v2.1`，head `f598c17`）；实施落点 `.github/workflows/quality.yml` job `drift-gate-runtime`；D-010（:8996 端口铁律）、D-019（v2.1 授权来源） | <!-- known-absent: f598c17 因 2026-08-02 filter-repo 失效 -->
| **来源备注** <!-- known-absent: 本行记录的是「原引用不存在」这一事实 --> | ⚠️ 2026-09-14 核查：本行原引用的 `doc/specs/drift-gate-runtime-v2.1-spec.md` 与 `doc/adr/0015-drift-gate-runtime-v2.1.md` **在仓库及全部 git 历史中均不存在**（ADR 0015 号已由 `0015-memory-store-health-observability.md` 占用），系引用了未落盘的规划文档。本决策的事实依据以 `quality.yml` 的实际 job 定义为准。同类幽灵引用另有 `reports/codex-handoff-closure-2026-08-01.md`（D-019 引用，仅存在于 tag 不在 HEAD）。 |
| **校验** | CI：`drift-gate-runtime` job 跑全量 verify.sh；废弃 :8996 有监听→[FAIL] 非零退出阻断合并，无监听→[PASS]。本地实测两种场景均正确（裸 TCP 探测，python3 优先、curl 兜底 fail-open） |
| **预期** | 任何在 :8996 起监听的漂移（DRIFT-2/3）在 PR 阶段即被拦；webui/memory-store 端口须与决策态一致（:8099/:8997） |
| **Drift** | —（运行时门禁已 fail-closed，无需额外兜底） |
| **Owner** | 审查协调 |
| **锁定** | 🔒（v2.1 运行时门禁已落地，PR #53） |
| **modified** | 2026-08-01｜by AI｜approved: 用户（v2.1 运行时门禁落盘，PR #53）；2026-08-01｜by AI｜approved: 用户（drift-gate-runtime 改并行独立跑：去掉 needs 依赖，避免被上游 skip；配套修正本条目事实行） |

---
## 待补充

### D-2026-08-01-082 | CDN SRI + 版本钉死（前端供应链防护）
| 字段 | 内容 |
|---|---|
| **事实** | 前端所有 CDN 第三方脚本均**钉死精确版本 + 带 SRI `integrity` 校验 + `crossorigin=anonymous`**，杜绝供应链篡改。涉及：lucide@1.25.0、katex@0.16.10、marked@12.0.0、dompurify@3.0.8（源 `cdn.jsdelivr.net` / `unpkg.com`） |
| **来源** | `services/webui/src/joy_interaction_webui/static/index.html:69-85`；`reports/frontend-review-20260723.md`（PR #6–#8 安全加固）；`doc/frontend/README.md:199`（短期已完成） |
| **校验** | 任何新增 CDN `<script>/<link>` 必须带 `integrity=` 且版本钉死、禁止 `@latest` 浮动；可由 `scripts/verify.sh` 加静态断言（`grep integrity=` 且版本非 `@latest`），当前靠前端测试 `services/webui/tests/test_webui_static_contract.py` + PR 评审 |
| **预期** | CDN 依赖始终版本钉死 + SRI；升级依赖时同步更新 `integrity` 哈希 |
| **Drift** | 无（已实现且稳定） |
| **Owner** | 审查协调 |
| **锁定** | 🔒 |
| **modified** | 2026-08-01｜by AI｜approved: 用户（解决 followup #4） |

### D-2026-08-01-083 | CSP 响应头（前端纵深防御）
| 字段 | 内容 |
|---|---|
| **事实** | webui `server.py` 对所有 HTTP 响应注入 `Content-Security-Policy`，限定 `script-src`/`style-src`/`font-src` 仅 `self` + `https://cdn.jsdelivr.net` + `https://unpkg.com`（含 `'unsafe-inline'` 兼容内联 style） |
| **来源** | `services/webui/src/joy_interaction_webui/server.py:1213-1225`；`doc/frontend/README.md:70` §5.2；`reports/code-health-audit-20260723.md:33`（`server.py:1103` 等处设 CSP） |
| **校验** | CSP 头不得删除/放宽；新增外部源须加入白名单。可由 `scripts/verify.sh` 加静态断言（`grep Content-Security-Policy`） |
| **预期** | 任何响应保留 CSP；放宽策略须同步更新白名单并过评审 |
| **Drift** | 无 |
| **Owner** | 审查协调 |
| **锁定** | 🔒 |
| **modified** | 2026-08-01｜by AI｜approved: 用户（解决 followup #4） |

### D-2026-08-01-084 | innerHTML 消毒纪律（前端 XSS 防护）
| 字段 | 内容 |
|---|---|
| **事实** | 所有动态 `innerHTML` 渲染必经 DOMPurify 消毒：Markdown 走 `render_markdown.js`（`DOMPurify.sanitize`），静态 HTML 走 `sanitize_static_html.js`（`sanitizeStaticHtml`/`sanitizeStaticCss`），`iframe.srcdoc` 亦过消毒。禁止未消毒的 `innerHTML =` 直写 |
| **来源** | `static/render_markdown.js:126-132`、`static/sanitize_static_html.js:53-72`；`reports/code-health-audit-20260723.md:43`（XSS 白名单纪律） |
| **校验** | 任何新增 `innerHTML` 路径必先过 `renderMarkdown` / `sanitizeStaticHtml`；可由 lint/测试断言兜底 |
| **预期** | 新增动态 HTML 渲染一律消毒；CSP 头勿删（与 SRI 构成纵深防御） |
| **Drift** | 无 |
| **Owner** | 审查协调 |
| **锁定** | 🔒 |
| **modified** | 2026-08-01｜by AI｜approved: 用户（解决 followup #4） |

### D-2026-08-01-085 | Push `.github/workflows/*` 需 `workflow` 作用域（运维）
| 字段 | 内容 |
|---|---|
| **事实** | 改动 `.github/workflows/` 下任何文件的推送，token 必须带 `workflow` OAuth 作用域（或 fine-grained `Workflows: Read and write`），否则 GitHub 拒收（`refusing to allow an OAuth App to create or update workflow ... without 'workflow' scope`）。本仓 `gh` CLI 默认 token 仅 `gist, read:org, repo`，缺 `workflow`，故改 workflow 文件须走 `github-pat` MCP 的 `push_files` 通道 |
| **来源** | 实测：PR #50 推 `quality.yml`（含 drift-gate job）时 `gh` push 被拒，改走 `github-pat` MCP（`push_files`）成功（commit `ffa721c`）；协作者记忆 `MEMORY.md`「MCP github-pat 通道」段（现位于 `.workbuddy/memory/MEMORY.md`，gitignore 内） | <!-- known-absent: ffa721c 因 2026-08-02 filter-repo 失效 -->
| **校验** | 推 workflow 文件前确认 token scope 含 `workflow`；否则改用 `github-pat` MCP |
| **预期** | 任何 workflow 文件改动经带 `workflow` scope 的通道落地 |
| **Drift** | 无 |
| **Owner** | 审查协调 |
| **锁定** | 🔒 |
| **modified** | 2026-08-01｜by AI｜approved: 用户（解决 followup #4） |

- D-XXX：model 文件不可重命名前缀（运维）  ← 待补（需先核实事实，不在本次 followup 范围）
