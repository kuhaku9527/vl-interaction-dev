# doc/architecture/ — 架构审计快照（2026-08-13 基线）

> **类型 B（事件型）**：本目录是 **2026-08-13 一次性审计**的产物，**快照不追改**。
> 其中的"22 个 JS 模块"等计数反映**当时**状态，随代码演进失效属正常。
>
> ⚠️ **现行架构/拓扑请读**：[`../runtime-topology.md`](../runtime-topology.md)（唯一权威）。
>
> **最近核对**：2026-09-14

| 文件 | 内容 | 被谁引用 |
|---|---|---|
| `codebase-map-2026-08-13.md` | 代码库模块地图 + 巨石文件解耦计划（370 行） | **代码 ×9**（`jarvis_*` 5 模块、`live_mode`、`tts_turn_common`、2 个测试） |
| `audit-frontend-2026-08-13.md` | 前端架构一致性审计 | **代码 ×4**（`ws_handler.py` + 3 个测试） |
| `audit-webui-backend-2026-08-13.md` | WebUI 后端只读代码审查 | **代码 ×3**（3 个测试文件） |
| `audit-webinfer-2026-08-13.md` | webinfer 只读代码审查 | 零外部引用（但属基线三件套之一） |

> **为何保留**：这批审计**被生产代码注释反向引用**（`codebase-map` 被 9 处引用），
> 删除会造成代码注释死链。属类型 C 特征，故不退役。
>
> **与 `reports/` 的分工**：`reports/` 放一次性交接（handoff/integration）；本目录放**被代码/文档引用的架构基线**。
