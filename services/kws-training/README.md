# KWS 自训 SOP（"bt"）— 历史快照

> **历史快照**：v3.2 P2 路线（详见 `doc/main/00-main-direction.md §4 #5`）原计划训练 "bt 在吗"。
> **2026-07-10 决策变更**：唤醒词已拍板为 **"bt"**（2 token B+T），自训 v4 模型已部署到 `D:\AI\models\sherpa-onnx\models\kws\bt-en\`。
> 本 README 是历史 SOP；新训练流程参见 `doc/subsystems/jarvis-mode.md §2.4`（v4 exp_v4 训练 4 轮迭代结果）。

> 重训时如要复制流程：把本文中"bt 在吗"全部换成"bt"，录音只录单字 "bt"（不必带中文），其他配置不变。