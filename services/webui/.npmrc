# 项目级 npm 配置 —— 跨项目隔离（2026-09-14）
#
# ── 背景 ──────────────────────────────────────────────────────────────
# 用户级环境变量 npm_config_cache 指向 D:\Workspace\hermes-agent\.cache\npm
# （那是另一个 agent 应用 hermes-agent 的缓存目录）。若不覆盖，本项目跑
# npm ci / npm test 会把产物写进别的项目，造成跨项目污染。
#
# ── ⚠️ 重要：本文件的局限性（实测）────────────────────────────────────
# npm 的配置优先级实测为：
#     env (npm_config_*)  >  project .npmrc  >  user .npmrc  >  builtin
# 即【环境变量高于项目 .npmrc】—— 与部分文档描述相反。
# 实测输出：cache = "<本项目>/.cache/npm" ; overridden by env
#
# 因此本文件【不足以】单独完成隔离，它是「声明意图 + 兜底」；
# 真正生效需要满足二者之一：
#   ① 清除/修正用户级 npm_config_cache（见 doc/environment-dsh.md §隔离）
#   ② 调用时显式传 --cache（CI 与本地命令均可用）
# ─────────────────────────────────────────────────────────────────────
cache=./.cache/npm
