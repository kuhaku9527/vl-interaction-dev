# ---------------------------------------------------------------------------
# quality.yml — CI quality gate (GitHub Actions).
#
# Triggers on every push and every PR targeting `main` and BLOCKS merge when
# the lint / format checks fail.
#
# Scope strategy
# --------------
# Lint is scoped to `services/webinfer` — the currently active development
# package — rather than the whole monorepo. The rest of the repo carries large
# amounts of pre-existing *decorative* lint debt (ambiguous-unicode strings in
# Chinese comments, PEP 585/604 style annotations, missing docstrings on every
# public symbol, test/loop naming, etc.) that would make a repo-wide gate red
# on day one and mask real signal.
#
# Within `services/webinfer` we still enforce the rules that catch *real* bugs
# and regressions (F = pyflakes undefined names / unused imports, E402 = import
# ordering, I = isort, B = bugbear, C4 = comprehensions, SIM = simplify, plus
# the few E/W basics). We `--extend-ignore` only the decorative rules that were
# measured to remain after `ruff --fix` + `ruff format` in the enabling PR
# (see `ruff check services/webinfer --statistics`). Those historical-debt
# rules are: UP007, D101, D102, D103, D205, D401, RUF005, RUF013, SIM105,
# SIM108. (RUF001/RUF003 were resolved by per-file `# ruff: noqa` annotations
# in the affected modules; RUF059/RUF046 and the SIM/RUF minor findings were
# fixed outright.)
#
# This keeps the gate green AND meaningful: it intercepts genuine defects in new
# code while forgiving cosmetic debt that is out of scope to chase down now.
# ---------------------------------------------------------------------------
name: Quality Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Allow deterministic manual dispatch. This repo has empirically shown that
  # pull_request/push events can occasionally fail to enqueue Actions runs
  # (stale refs/pull cache on force-pushed branches); workflow_dispatch is a
  # reliable fallback to trigger the gate via the REST API.
  workflow_dispatch:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install ruff
        run: pip install ruff==0.15.22
      - name: Ruff lint (scoped to services/webinfer)
        # `--extend-ignore` adds to the repo-wide [tool.ruff.lint] select set
        # in pyproject.toml; it only suppresses the decorative historical-debt
        # rules measured in the enabling PR. Real-bug rules (F/E402/I/B/C4/SIM)
        # stay active.
        # UP007 was dropped: the Optional[X] -> X | None debt (86 sites) was
        # burned, so the rule no longer fires. RUF059/RUF046/RUF022 are new
        # decorative rules introduced by ruff 0.15.22 and are not real bugs.
        run: ruff check services/webinfer --extend-ignore D101,D102,D103,D205,D401,SIM105
      - name: Ruff format check (scoped to services/webinfer)
        run: ruff format --check services/webinfer

      # --- Phase 2 expansion: lint the remaining Python services -----------
      # Every package keeps the repo-wide real-bug rules (F/E402/I/B/C4/SIM/
      # N/RUF/UP/D) active; only its decorative historical-debt rules (measured
      # during the enabling pass) are suppressed via --extend-ignore. webui's
      # Python suite now carries its own [tool.ruff.lint] config (real-bug rules
      # enabled, docstring + other decorative debt ignored), so it is linted
      # directly from its pyproject without CLI --extend-ignore overrides.
      - name: Ruff lint (scoped to services/memory-store)
        run: ruff check services/memory-store --extend-ignore D101,D102,D103,RUF001,SIM105
      - name: Ruff format check (scoped to services/memory-store)
        run: ruff format --check services/memory-store
      - name: Ruff lint (scoped to services/voice-clone)
        run: ruff check services/voice-clone --extend-ignore RUF001,D102,D103,D105,D401,D205,SIM105
      - name: Ruff format check (scoped to services/voice-clone)
        run: ruff format --check services/voice-clone
      - name: Ruff lint (scoped to services/asr)
        run: ruff check services/asr --extend-ignore D103,D101,SIM105
      - name: Ruff format check (scoped to services/asr)
        run: ruff format --check services/asr
      - name: Ruff lint (scoped to services/tts)
        run: ruff check services/tts --extend-ignore D103,SIM105,D102,D101,RUF001
      - name: Ruff format check (scoped to services/tts)
        run: ruff format --check services/tts
      - name: Ruff lint (scoped to services/background-agent)
        run: ruff check services/background-agent --extend-ignore D101,D103,D102,D205
      - name: Ruff format check (scoped to services/background-agent)
        run: ruff format --check services/background-agent
      - name: Ruff lint (scoped to services/webui Python)
        # Run from services/webui so ruff resolves webui's OWN [tool.ruff.lint]
        # config (line-length=100, target py310) instead of the repo-root config.
        working-directory: services/webui
        run: ruff check .
      - name: Ruff format check (scoped to services/webui Python)
        working-directory: services/webui
        run: ruff format --check .

  # ---------------------------------------------------------------------------
  # drift-gate (F4-P0 / ADR-0017): 配置/代码级静态契约守卫（fail-closed）。
  # 刻意「不 needs」任何其它 job（含 ruff），保持独立运行——
  # 避免因上游红而被 skipped（已知 runner 配额耗尽会全 FAILURE 的陷阱，
  # 见 ADR-0016 / 决策历史）。drift_gate.py 是单一真值（契约驱动、机器可读）；
  # runtime 阶段（vlm-n_ctx）依赖运行态，留给下方的 drift-gate-runtime job。
  # 引用文件缺失（如 .gitignore 忽略的 run-windows.env）按 fail-open 处理，CI 保持绿。
  #   ⚠️ 2026-09-23（#159）例外：4 条决策评测 check 显式写了
  #      present_when_missing="fail"，其引用的**结果产物**缺失即判红（缺结果
  #      = 没测 ≠ 通过）。其余 check 的缺省 skip 语义一个字段都没动。
  # ---------------------------------------------------------------------------
  drift-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Generate decision-eval scorecards (stable paths, before the gate)
        # #159（父 spec #154 的决策评测⑤）：沿用既有先例形态接线 ——
        # 「产物写到稳定路径 → 门禁断言产物」，**不**新增第二个执行器、
        # **不**新增 job、**不**加 needs。门禁读的是产物，它自己不跑卡片。
        #
        # ★ 退出码取舍（如实记录）：卡片的退出码是 0=全绿 / 1=有判据判红 /
        #   2=有「无法测量」项。定向轴当前**实测 exit=1** —— D4
        #   （not_for_me_recall_pct 泛化召回 0.0 < 17.0）未达标，那是 #157
        #   的**如实读数**，不是本次接线的缺陷。若用 set -e 直接失败，CI 会因为
        #   「读数如实」而红，等于把门禁的判定权搬进了生成步骤；而把非零退出
        #   一律当成功又会把真实崩溃吞掉。故逐码分辨：
        #     0 全绿            → 放行，交下一步 drift_gate 按产物内容判
        #     1 有判据判红      → 放行，同上（已知红固定在契约里；判定权在门禁）
        #     2 有「无法测量」项 → **失败** ★「没测 ≠ 通过」，不得放行
        #     其它             → **失败**（这不是一个读数，卡片坏了）
        # ★ R6 修复：生成前先 `rm -f` 两个目标产物。
        #   原实现在卡片「无法出卡」时 rc=2 且**一个字节都没写**，而步骤只检查
        #   「文件存在且可解析」⇒ **旧产物**被当成新产物 ⇒ 步骤 rc=0、
        #   门禁 rc=0、CI 全绿而卡片从未产出结果（t4 对抗复核实测）。
        # ★ t8（AC#6）改为「**临时目录生成 + 成功后原子替换**」：
        #   `rm -f` + 原地写会在**卡片失败时破坏工作区**（t7 实测：卡片失败时
        #   STEP-RC=1，但定向卡已被重写、时序卡被删）。现在卡片只写 `$STAGE`，
        #   两张都校验通过后才 `mv` 到入库路径 ⇒ 失败时入库产物**逐字节不变**。
        #   判据由 scripts/tests 里的 CI 步骤测试钉住（失败后产物仍在且 md5 不变）。
        # ★ t8（AC#5）：把产物的 **sha256** 打进 CI 日志（不再只有 size）。
        #   这样「同一次 CI 内生成 → 断言」的对象事后可核验：拿到内容指纹就能确认
        #   被门禁断言的到底是哪一份产物。
        # ★ 生成命令与本地**逐字一致**，只有解释器不同（本地
        #   /d/AI/envs/joyai-main/python.exe ↔ CI python）。CI 是 ubuntu+py3.12，
        #   故不得出现任何 Windows 专有路径。
        run: |
          set -u
          DIRECTED=doc/research/data/decision_eval_directed_card.json
          TIMING=doc/research/data/decision_eval_timing_card.json
          # 暂存目录：卡片只写这里，入库路径在全部校验通过后才被替换。
          # `${RUNNER_TEMP:-/tmp}` 使本步骤在 CI 与本地 shell 里行为一致
          # （RUNNER_TEMP 是 GitHub runner 上的变量；本地跑时回落到 /tmp）。
          STAGE="$(mktemp -d "${RUNNER_TEMP:-/tmp}/eval-cards.XXXXXX")"
          D_STAGE="$STAGE/directed.json"
          T_STAGE="$STAGE/timing.json"
          rc_directed=0
          rc_timing=0
          python services/webinfer/decision_eval_card.py --json --out "$D_STAGE" || rc_directed=$?
          python services/webinfer/decision_eval_timing_card.py --json --out "$T_STAGE" || rc_timing=$?
          echo "card exit codes: directed=$rc_directed timing=$rc_timing (0=全绿 1=有判据判红 2=有无法测量项)"
          for pair in "directed:$rc_directed" "timing:$rc_timing"; do
            name=${pair%%:*}; rc=${pair##*:}
            case "$rc" in
              0) ;;
              1) echo "[ok] $name: rc=1 —— 如实判红，判定权交给 drift_gate（按产物内容判）" ;;
              2) echo "::error::$name card rc=2 —— 有「无法测量」项。没测 ≠ 通过，本步骤失败。"; echo "  入库产物未被改动（本步骤只写暂存目录）"; exit 1 ;;
              *) echo "::error::$name card exited $rc — 这不是一个读数，卡片未正常产出"; echo "  入库产物未被改动（本步骤只写暂存目录）"; exit 1 ;;
            esac
          done
          # 校验**暂存**产物：非空 + 可解析 + kind/ticket 对得上。
          # 只在全部通过后才替换入库路径 ⇒ 失败时工作区零改动。
          python - "$STAGE" <<'PY'
          import hashlib
          import json
          import os
          import sys
          stage = sys.argv[1]
          expected = [
              ("directed", "directed-axis-scorecard", "#157"),
              ("timing", "timing-axis-scorecard", "#158"),
          ]
          for name, kind, ticket in expected:
              path = os.path.join(stage, f"{name}.json")
              try:
                  size = os.path.getsize(path)
              except OSError as exc:
                  print(f"::error::评测产物缺失（卡片未写出）: {name} ({exc})")
                  print("  入库产物未被改动（本步骤只写暂存目录）")
                  sys.exit(1)
              if size == 0:
                  print(f"::error::评测产物为空文件: {name}")
                  sys.exit(1)
              raw = open(path, "rb").read()
              try:
                  data = json.loads(raw.decode("utf-8"))
              except (json.JSONDecodeError, UnicodeDecodeError) as exc:
                  print(f"::error::评测产物不可解析: {name} ({exc})")
                  sys.exit(1)
              if data.get("kind") != kind or data.get("ticket") != ticket:
                  print(f"::error::{name} 不是预期的记分卡产物 "
                        f"(kind={data.get('kind')!r} ticket={data.get('ticket')!r})")
                  sys.exit(1)
              # ★ AC#5：内容指纹进日志（不只 size）—— 事后可核验被断言的是哪一份
              print(f"[ok] {name}: kind={kind} ticket={ticket} "
                    f"cards={len(data.get('cards', {}))} bytes={size} "
                    f"sha256={hashlib.sha256(raw).hexdigest()}")
          PY
          # ★ 校验失败必须**拦住替换**。本步骤是 `set -u`（不是 `set -e`），
          #   所以 heredoc 里 `sys.exit(1)` 不会自动中止脚本 —— 不显式取 rc 的话，
          #   校验判红后仍会执行下面的 `mv`，把坏产物盖到入库路径上
          #   （t8 实测：空产物场景 rc=0 且产物被替换；由
          #   test_failure_leaves_committed_artifacts_byte_identical 抓出）。
          if [ "$?" -ne 0 ]; then
            echo "::error::产物校验失败 —— 不执行替换"
            echo "  入库产物未被改动（本步骤只写暂存目录）"
            rm -rf "$STAGE"
            exit 1
          fi
          # 暂存产物全部校验通过 ⇒ 原子替换入库路径（同一文件系统内 `mv` 是原子的）。
          mv -f "$D_STAGE" "$DIRECTED"
          mv -f "$T_STAGE" "$TIMING"
          rm -rf "$STAGE"
          echo "[ok] 两张产物已原子替换入库路径"
      - name: Run drift-gate static contract checks (fail-closed)
        # F4-P0: 改用 scripts/drift_gate.py 作为单一真值（替代旧的 verify.sh --ci
        # grep 子集）。仅跑 static 阶段（查配置/代码常量，无需起服务），任一
        # block 级检查不符即非零退出、阻断合并。
        # #159：本 job 现在还会断言上一步生成的两张记分卡产物（4 条 eval-* check，
        # 均为 block + present_when_missing=fail）。评测 check 刻意用 phase=static：
        # 执行器对 static 是纯读文件 + re.search，不碰运行实例、不触发 VLM probe；
        # 而「结果文件」本就是已落盘产物，对执行器而言正是 static 的语义。
        # （若标成 runtime，_props_referenced_by_runtime 会因既有的 vlm-n_ctx 而
        #   在每次 --phase runtime 前无条件跑 probe，无 llama 时 rc=3 ⇒ CI 永远跑不了。）
        run: python scripts/drift_gate.py --contract config/drift-contract.json --phase static --mode closed --no-history
      - name: Drift-gate smoke test (regression guard for P0-1 UnboundLocalError)
        # 唯一能防 drift_gate.py 非 --json 路径崩溃（P0-1 修复的 UnboundLocalError）
        # 回归的自动化。smoke test 对缺失文件/env 均 fail-open，可在 CI 跑。
        run: python scripts/drift_gate_smoke_test.py

  # ---------------------------------------------------------------------------
  # drift-gate-runtime (Drift Gate v2.1 阶段A)：起 webui(:8099)+memory-store(:8997)
  # 两个 CPU-only 服务，跑全量 verify.sh，对这两服务的端口检查（含废弃 :8996
  # 反向断言）fail-closed，拦下 DRIFT-2/3 类"监听错端口"漂移。其余 6 个服务
  # (VLM/ASR/TTS/Hermes/background-agent 等) 在 CI 未起 -> [DOWN] 不计 fail
  # （沿用 check_port 语义）。决策记录见 `决策/跨域铁律.md`（Drift Gate v2.1 条目）。
  # 注：2026-09-14 核查，早前此处引用的 doc/adr/0015-drift-gate-runtime-v2.1.md 与
  # doc/specs/drift-gate-runtime-v2.1-spec.md 从未落盘，引用已移除。
  # 注意：本 job 刻意「不 needs drift-gate」，与静态门禁并行独立跑——
  # 避免因上游红而被 skipped（之前 needs 链导致 runtime 门禁从未在 CI 真跑过，
  # 见 PR #53 run 30703927856）。两者正交：静态(drift_gate.py) vs 运行时(全量)。
  drift-gate-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install memory-store (decision-state deps)
        run: |
          cd services/memory-store
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Install webui Python deps
        run: |
          cd services/webui
          python -m pip install --upgrade pip
          pip install -e .

      # 起两服务 + 轮询 /health 就绪 + 跑全量校验，必须在同一 step 内完成，
      # 否则后台进程会在 step 结束时被回收，verify.sh 探不到端口 -> 误报。
      - name: Start services and run full drift verification (fail-closed)
        env:
          MEMORY_PORT: "8997"            # D-040/041 决策态端口（memory-store 默认是废弃 8996，须显式覆盖）
          MEMORY_HOST: "127.0.0.1"
          MEMORY_SQLITE_PATH: "/tmp/joyai-memory.sqlite"
          MEMORY_VEC_DIR: "/tmp/joyai-memory-vec"
          JOYAI_MEMORY_STORE_URL: "http://127.0.0.1:8997"   # D-032 webui 网关指向决策态 8997
        run: |
          set -u
          cd "$GITHUB_WORKSPACE"

          # 启动 memory-store（决策态 :8997）
          python -m memory_store.app > /tmp/memory-store.log 2>&1 &
          MS_PID=$!

          # 启动 webui（决策态 :8099；HTTP 不加 SSL 以便 /health 探测）
          ( cd services/webui && PYTHONPATH=src python -m joy_interaction_webui.server \
              --host 127.0.0.1 --port 8099 --no-ssl ) > /tmp/webui.log 2>&1 &
          WEBUI_PID=$!

          wait_for() {
            local url="$1" name="$2" deadline=$(( SECONDS + 30 ))
            echo "Waiting for ${name} at ${url} ..."
            while (( SECONDS < deadline )); do
              local code
              code=$(curl -s -o /dev/null -w "%{http_code}" -m 2 "$url" || true)
              if [ "$code" = "200" ]; then echo "Ready: ${name}"; return 0; fi
              if ! kill -0 "$MS_PID" 2>/dev/null; then
                echo "memory-store exited early"; echo "--- memory-store.log ---"; cat /tmp/memory-store.log; return 1
              fi
              if ! kill -0 "$WEBUI_PID" 2>/dev/null; then
                echo "webui exited early"; echo "--- webui.log ---"; cat /tmp/webui.log; return 1
              fi
              sleep 1
            done
            echo "TIMEOUT waiting for ${name} (${url})"
            echo "--- memory-store.log ---"; cat /tmp/memory-store.log
            echo "--- webui.log ---"; cat /tmp/webui.log
            return 1
          }

          wait_for "http://127.0.0.1:8997/health" "memory-store" || exit 1
          wait_for "http://127.0.0.1:8099/health" "webui" || exit 1

          # 全量校验：webui/memory-store 端口检查 fail-closed；
          # 其余服务未起 -> [DOWN] 不计 fail。任何 [FAIL] -> 非零退出 -> 阻断合并。
          bash scripts/verify.sh
          RC=$?
          kill "$MS_PID" "$WEBUI_PID" 2>/dev/null || true
          exit $RC

  package-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build backend
        run: python -m pip install --upgrade pip build
      - name: Build wheel
        working-directory: services/webinfer
        run: python -m build --wheel
      - name: Install wheel into a clean venv
        run: |
          python -m venv /tmp/webinfer-smoke-venv
          /tmp/webinfer-smoke-venv/bin/python -m pip install --upgrade pip
          /tmp/webinfer-smoke-venv/bin/python -m pip install services/webinfer/dist/*.whl
      - name: Import smoke test
        run: |
          /tmp/webinfer-smoke-venv/bin/python -c "import live_adapter; from live_adapter import StreamingInferAdapter, AdapterConfig"
          /tmp/webinfer-smoke-venv/bin/python -m pip show joyvl-webinfer-adapter
      - name: App construction smoke test
        run: /tmp/webinfer-smoke-venv/bin/python -c "from live_adapter import create_app, parse_args; app = create_app(parse_args()); assert app is not None"
      - name: Console script smoke test
        run: /tmp/webinfer-smoke-venv/bin/joyvl-webinfer-adapter --help

  eslint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/webui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm run lint

  frontend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/webui
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm test

  # ---------------------------------------------------------------------------
  # scripts-tests (工单 #162 / #163): 验证仪式运行器 + 帧链路探针的行为测试。
  # 这两个工具的输出都会变成台账证据 —— 一条假的绿色行比没有台账更坏（#162 的
  # 出发点是「残留审计在静态服务器服务错目录时报 139 假数字，真值 0」）。
  # 故这些测试**必须在 CI 可见处**：放在 services/*/tests 之外的文件不会被上面
  # 任何 job 收集，等于没有测试（#155 的教训 —— 计分器曾因不在矩阵内而长期零覆盖）。
  # 纯离线：用注入的假探针/假执行器 + 纯函数判据，不起任何服务、不开浏览器。
  #
  # ★ 跑**整个目录**（而不是逐个点名文件）：点名写法会让「新加的测试文件」静默
  #   不被收集 —— 那正是本仓已经踩过的病（#152：46 个测试从未被收集）。
  # ---------------------------------------------------------------------------
  scripts-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # 帧链路探针是 .mjs，其行为测试需要 node 才能跑（缺失时会 skip —— 而
      # 「skip 了却看起来绿」正是本仓 §6.4 fails-open 的一类）。故显式装 node。
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      # 同一个理由也适用于 ruff：`scripts/tests/test_ci_ruff_runner.py` 会真的
      # 调用 ruff（它钉的是「ruff 对不存在的路径退出 0」这一**实测行为**，
      # 以及本运行器的退出码契约）。不装 ruff 时那些测试会以
      # ModuleNotFoundError 的形式失败，而报错信息看起来像「ruff 改了行为」
      # —— 一个指错方向的告警。实测发生过一次（run 35835923523）。
      # ★ 版本与 ruff job 同钉 0.15.22：本仓行为测试依赖具体的 ruff 行为，
      #   浮动版本会让「实测钉住的行为」在无人改动时失效。
      - name: Install pytest and pinned ruff
        run: pip install pytest ruff==0.15.22
      - name: Scripts tests (offline, no services)
        run: python -m pytest scripts/tests/ -q

  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: [memory-store, background-agent, webinfer, webui, tts]
    steps:
      # fetch-depth: 0 (full history) is REQUIRED by this job, not an
      # optimization choice. `tests/test_qa_server_split_equivalence.py` pins the
      # pre-split server.py by retrieving it from git history
      # (`git show 5c0089e~1:...`), i.e. ~149 commits deep. The actions/checkout
      # default is fetch-depth: 1, under which that object is simply absent and
      # all 52 of its cases ERROR out ("invalid object name '5c0089e~1'") — a
      # CI-only failure mode that cannot reproduce in a normal full clone.
      # The repo pack is ~11 MiB, so a full fetch is cheap.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test dependencies
        run: |
          cd services/${{ matrix.service }}
          pip install --upgrade pip
          if [ "${{ matrix.service }}" = "webinfer" ]; then
            # webinfer declares its runtime + dev deps in its own pyproject;
            # install editable so the suite imports the checked-out source
            # (aiohttp, aiohttp-cors, Pillow, openai, httpx, pytest,
            # pytest-asyncio) rather than the shared lightweight set below.
            pip install -e ".[dev]"
          elif [ "${{ matrix.service }}" = "memory-store" ]; then
            # memory-store declares usearch + numpy (ADR-0012 vector recall) in
            # its own pyproject; editable install pulls them in alongside pytest.
            pip install -e ".[dev]"
          elif [ "${{ matrix.service }}" = "webui" ]; then
            # webui's pyproject declares its runtime deps (aiohttp, aiohttp-cors,
            # aiortc, opencv-python, numpy, Pillow, openai, httpx, av, psutil,
            # onnxruntime, transformers) in [dependencies] and test deps
            # (pytest, pytest-asyncio) in [dev]. server.py imports aiohttp +
            # aiortc at module top-level and the existing tests also import
            # Pillow/httpx, so a minimal install set fails at collection with
            # ModuleNotFoundError. Install editable to pull in the full declared
            # dependency graph in one shot — a minimal set is not sufficient.
            pip install -e ".[dev]"
          elif [ "${{ matrix.service }}" = "tts" ]; then
            # services/tts/tts_adapter.py imports websockets at module top-level,
            # so the suite cannot be collected without it; install the lightweight
            # set + websockets so the tests import successfully.
            pip install fastapi pydantic "uvicorn[standard]" httpx websockets pytest pytest-asyncio
          else
            pip install fastapi pydantic "uvicorn[standard]" httpx pytest pytest-asyncio
          fi
      - name: Run pytest
        run: |
          cd services/${{ matrix.service }}
          python -m pytest -o asyncio_mode=auto -q
